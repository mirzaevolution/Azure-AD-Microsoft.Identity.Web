
@{
	ViewData["Title"] = "AES Decrypt";
}

<h3>AES Decrypt</h3>
<div class="row">
	<div class="col-md-5">
		<form asp-action="Decrypt">
			<div class="form-group">
				<label>Plain Text</label>
				<input type="text" class="form-control" placeholder="Enter your cipher text" required name="cipherText" />
			</div>
			<div class="form-group">
				<button type="submit" class="btn btn-primary">Decrypt</button>
			</div>
		</form>
		@if (!string.IsNullOrEmpty(ViewBag.Result))
		{
			<div>
				<p>Result: @ViewBag.Result</p>
			</div>
		}
	</div>
</div>